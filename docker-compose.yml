services:
  newapi-video-tester:
    build:
      context: .
      dockerfile: Dockerfile
    image: newapi-video-tester:latest
    container_name: newapi-video-tester
    ports:
      - "5173:80"
    restart: unless-stopped
